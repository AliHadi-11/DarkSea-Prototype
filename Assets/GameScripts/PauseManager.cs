// Replaced by PauseMenuUI.cs (UI Toolkit).
// This stub keeps the class name so old scene references don't break.
// Remove this component from scene GameObjects and add PauseMenuUI instead.
public class PauseManager : UnityEngine.MonoBehaviour { }
